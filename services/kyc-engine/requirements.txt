# kyc-engine runtime (SPEC A §8 pins). Heavy CPU-inference deps install in the
# Docker image; the service degrades gracefully ([SIM]-tagged fallbacks) when
# they are absent (e.g. light CI) — see requirements-test.txt for the minimal
# test set.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
SQLAlchemy>=2.0
psycopg[binary]>=3.1            # Postgres prod (DATABASE_URL)
httpx>=0.27
numpy>=1.26
pillow>=10.0
cryptography>=42.0              # Keycloak RS256 verification
boto3>=1.34                     # MinIO/S3 WORM storage (MINIO_URL set)
kafka-python>=2.0.2             # evidence outbox relay (KAFKA_BOOTSTRAP set)
python-multipart>=0.0.9         # multipart uploads
temporalio>=1.8                 # TEMPORAL_URL set -> durable workflows
# --- CPU inference stack (image build; ~1.9GB) ---
docling>=2.0
paddlepaddle==2.6.*
paddleocr>=2.8,<4.0  # 2.x & 3.x both supported (stage_ocr is signature-tolerant); 4.x unvetted
onnxruntime==1.19.*
insightface==0.7.3
mediapipe==0.10.*
ollama>=0.3.0
